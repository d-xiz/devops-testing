# Normalize line endings across all systems
* text=auto

# Enforce LF endings for source code
*.js    text eol=lf
*.html  text eol=lf
*.css   text eol=lf
*.json  text eol=lf

# Mark node_modules as vendored so GitHub Linguist ignores it
node_modules/** linguist-vendored

# Treat images as binary (no diffs)
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary