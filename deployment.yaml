apiVersion: apps/v1
kind: Deployment
metadata:
  name: chess-club-deploy # Name of the deployment
spec:
  replicas: 2 #specifies how many identical replicas (pods) of the application should be maintained, in this case, 2, which means Kubernetes will ensure that two pods running the specified container are always available
  selector:
    matchLabels:
      app: chess-club # Selector to match pods managed by this deployment
  template:
    metadata:
      labels:
        app: chess-club # Labels applied to pods created by this deployment
    spec:
      containers:
        - name: chess-club # Name of the container
          image: chess-club-app # Docker image to use for the container
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000 # Port that the container exposes
          env:
            - name: NODE_ENV
              value: "production"
